# 问题清单（Issue Log）

> 规则：问题必须可复现、可归属、可关闭。

## Open
- [ ] **ISS-007** `P3` `RetryPushConsumer.java:52` `memberUidList` 取 sender uid 与 retry 语义不符（已知遗留 bug,与 ISS-006 同文件域,留作单独跟进）
  - 发现：ISS-006 调查时副发现
  - 现象：`RetryPushConsumer` 处理「receiver 未 ACK」重推语义,但调 `contactDao.refreshOrCreateActive` 时 `memberUidList` 用 `fromUser.uid`(sender),应是 receiver uid 列表
  - 影响：retry 路径推进的是 sender 的 contact 行而非未 ACK 的 receiver,与意图相反;不影响主路径(主路径由 AckConsumer 推进)
  - 处置：观察是否有 retry 路径相关的 contact 偏移投诉,有则升级 P2
  - Owner：server-dev

- [ ] **ISS-015** `P1` 聊天界面消息排序异常——用户发新消息后消息列表被重新分组排序 — 2026-05-14
  - 发现：manager 2026-05-14，ui-tester 复测 4-bug 闭环时副发现
  - 现象：与安洁（AI 助理）对话时，发新消息后所有用户消息被重排到 AI 回复之上，打乱时间顺序。例：原序「用户1 → 安洁1 → 安洁2」发送用户2 后变为「用户1 → 用户2 → 安洁1 → 安洁2」
  - 根因：`src/stores/chat.ts` 中 `chatMessageList`、`chatMessageListByRoomId`、`clearRedundantMessages` 三处均按 `Number(message.id)` 排序。`message.id` 为 snowflake，IM Server 与 AI Bridge（aichat-node）可能使用不同 generator / 节点 ID，导致 ID 区段不连续；按 ID 排序时同一发送方消息被集中排在一起，违背时间序。
  - 修复（commit `de8faf0eb`）：3 处排序全部改为按 `message.sendTime`（发送时间戳）排序，加 `?? 0` 防御空值。1 文件 +5/-4。
  - 验证：待 ui-tester 复测——与安洁对话，发送用户消息后，所有消息应严格按发送时间先后排列，不再出现按发送人分组错乱。
  - Owner：frontend-dev

- [ ] **ISS-013** `P3` Maven 编译期 charset 未显式配置——根 pom 缺 `project.build.sourceEncoding=UTF-8`
  - 发现：server-dev 2026-05-13，跟进 ui-tester 报告的 unicode 乱码时定位
  - 根因：项目四个 pom 均未设 `project.build.sourceEncoding` / `maven.compiler.encoding`，javac 用平台默认 charset 编译 UTF-8 源文件 → 中文字面量在 class 常量池已乱码 → 所有中文 String literal 受影响
  - 修复：根 pom `<properties>` 加 3 行 UTF-8 声明 + `mvn clean` 全量重编验证
  - Owner：server-dev

- [ ] **后续重构**（非阻塞）：`ChatServiceImpl.syncContactLastMsgId` 在 ISS-006 后已不再单纯 sync `last_msg_id`,也推进 `active_time`,方法名误导。下一次需要改这一段时一并改名。Reviewer 在 ISS-006 PR #3 指出,server-dev 在 ISS-005 PR #4 确认。

## Closed
- [x] **ISS-009** `P1` 登录后主界面报"查询消息失败" — 2026-05-13
  - 根因：后端 `RoomAppServiceImpl.getFriendRoomMap:1386` `Collectors.toMap` 在 `im_user.is_del=1` 但 `im_room_friend.is_del=0` 时 value 为 null 触发 JDK NPE
  - 后端修复（PR #6, `8735dcde`）：`getFriendRoomMap` 跳过已删用户 + `getRoomBaseInfoMap` FRIEND/GROUP 占位 + `buildContactResp` null 防御共 5 处
  - 前端兜底（commit `8f872d2bb`）：`isError` 标记 + 错误卡片 + 重试按钮，WS 推送自动恢复
  - 评审：reviewer 复核通过（与 ISS-003 无关，是历史遗留被已删 AI 数据暴露）
  - 验证：backend-tester + ui-tester curl 复测 `/api/im/chat/contact/list` 200 + 10 项
  - Owner：server-dev（主修），frontend-dev（兜底 UX）

- [ ] **ISS-012** `P3` Dawn 账号 7 个 AI 测试账号 `im_user.is_del=1` 但 `im_room_friend.is_del=0` 数据不一致
  - 发现：server-dev ISS-009 排查 2026-05-13，定位 NPE 时发现
  - 现象：Dawn (uid=10937855681024) 的 7 个 AI 测试好友在 `im_user` 表已逻辑删除，但 `im_room_friend` 未级联软删
  - 影响：接触列表查询时行为不一致；可能不是孤例
  - 处置：短期 DBA 跑对齐脚本；长期改删用户 service 加级联软删
  - Owner：server-dev / backend-tester

- [ ] **后续重构**（非阻塞）：`ChatServiceImpl.syncContactLastMsgId` 在 ISS-006 后已不再单纯 sync `last_msg_id`,也推进 `active_time`,方法名误导。下一次需要改这一段时一并改名。Reviewer 在 ISS-006 PR #3 指出,server-dev 在 ISS-005 PR #4 确认。

## Closed
- [x] **ISS-010** `P2` AI 助理列表与好友列表状态不一致 — `SmokeTestAI-renamed` — 2026-05-13
  - 根因：前端把两个互不相关的字段当作同一个语义渲染:
    - `/aiclaw/list.authStatus` 来自 `im_aiclaw.auth_status` DB 列, 表示「激活生命周期」(0=未激活 / 1=已激活 / 2=已停用)
    - `/chat/contact/list.activeStatus` 来自 Redis ZSET, 表示「运行时在线状态」(OnlineEnum.ONLINE=1 / OFFLINE=2)
    前端 aiclaw 列表把 `authStatus=1` 映射成 i18n `aiclaw.status.online` (在线), 已激活但当前离线的助理在 aiclaw 列表看到「在线」, 在好友列表看到「离线」, 表现为状态不一致
  - 方案: A1 — 后端 `/aiclaw/list` 加 `activeStatus` 字段, 前端两个数据源各管各的
  - 修复:
    - 后端 PR #7 (server-dev, 已 merge to dev): `AiclawListResp` 新增 `activeStatus` 字段, 从 Redis ZSET 实时读取
    - 前端 commit `df7ea6c43` (5 文件 +106/-39):
      - `src/views/aiAssistantWindow/index.vue` (桌面): `AiclawListItem` 加 `activeStatus?: number`; 列表副标题 + 详情面板主徽章改读 `activeStatus`; `authStatus` 仅在非「已激活」(=1) 时降级为二级小标签
      - `src/mobile/views/my/AiAssistant.vue` (移动列表): 同上模式; 右侧按钮区原本用 `aiclaw.token.{view,activated,deactivated}` 表达 `authStatus`, 语义本就正确, 不动
      - `src/mobile/views/my/AiAssistantDetail.vue` (移动详情): 同桌面详情面板模式
      - `locales/{zh-CN,en}/aiclaw.json`: 新增 `auth_status.{inactive,activated,deactivated}` 双语
    - 兼容: `activeStatus?: number` 可选, 老后端响应无此字段时前端按离线降级处理, 不抛错
    - 不动: token-view 按钮门控 `authStatus === 0` 与 `FriendInfo.vue` 的 `authStatus === 2` 检查均语义正确, 保留
  - 验证: 待 ui-tester 复测 — `SmokeTestAI-renamed` 在 AI 助理弹窗与好友列表应显示相同的在线/离线状态
  - Owner：server-dev (后端 A1) / frontend-dev (前端 A1)
- [x] **ISS-008** `P1` Windows 客户端主界面关闭按钮无响应 — 2026-05-14（双 bug 闭环）
  - 一阶根因：`ActionBar.vue` 关闭确认弹窗内两个 `<n-radio>` 用 `:checked` + `@change="tipsRef.type = ..."` 单独绑定，未包在 `<n-radio-group v-model:value>` 内。在 naive-ui v3 下 `@change` 事件未稳定回调，选择「退出应用」后 `tipsRef.type` 仍保持默认值，`handleConfirm` 走到 `HIDE` 分支而不是 `EXIT`。
  - 一阶修复（commit `5ec53a609`）：`src/components/windows/ActionBar.vue` 改用 `<n-radio-group v-model:value="tipsRef.type">` 包裹两个 `<n-radio :value="...">`，让选中值同步流回 `tipsRef.type`。1 文件 +12/-8。
  - 一阶复测（2026-05-14 ui-tester）：radio 写回正确，`handleConfirm` 进入 EXIT 分支并 `emit(EventEnum.EXIT)`，但主程序仍不退出，托盘残留。
  - 二阶根因（ui-tester 排查 + frontend-dev 验证）：`src/App.vue` 顶部 `appWindow` 通过 `import('@tauri-apps/api/webviewWindow').then(...)` 异步赋值，`onMounted` 同步执行时 `appWindow.value` 仍为 `null`，旧代码 `appWindow.value.listen(EventEnum.EXIT, ...)` 抛 `TypeError`，EXIT 监听**永不注册** → ActionBar 用全局 `emit(EventEnum.EXIT)` 广播后无回调 → `exit(0)` 不触发。两 bug 互相独立：一阶补好 radio 后才暴露二阶。
  - 二阶修复（commit `1d6511e22`）：`src/App.vue:715-723` 把 `appWindow.value.listen(EventEnum.EXIT, ...)` 改成顶部已 import 的全局 `listen(EventEnum.EXIT, ...)`（来自 `@tauri-apps/api/event`，与 ActionBar 全局 `emit` 对称），`addListener(..., 'app-exit')` 包裹保留以走 useTauriListener 清理。1 文件 +3/-1。
  - 验证（2026-05-14 ui-tester GUI 复测 PASS）：启动 → 登录 → 关闭主窗口 → 勾选「退出应用」→ 确定 → 进程完全退出（`Get-Process hula` 空，任务栏无残留）。一阶 + 二阶双 bug 全部闭环。
  - Owner：frontend-dev

- [x] **ISS-011** `P2` 记住密码功能失效 — 2026-05-13
  - 根因：桌面端 `useLogin()` 在登录窗口和 home 窗口是**两个独立的 Vue app 实例**（Tauri 多窗口架构），登录窗口里用户输入的 `info.value.password` 在 home 窗口的 `init()` 里不可见 → `historyEntry.password` 写入空字符串 → pinia-plugin-persistedstate 持久化空值 → 退出登录后登录界面没有密码可回填。
  - 修复（commit `fdc62cdd0`）：`src/hooks/useLogin.ts` 增加跨窗口 `localStorage.__pendingHistoryPassword` 桥接：
    1. 登录窗口 `normalLogin().then()` 在登录**成功**分支内写入 `{password, ts}`（失败分支不写，避免毒化历史）。
    2. home 窗口 `init()` 读取时校验 30s TTL（覆盖窗口创建 + WS 连接 + 两次 HTTP 调用，但避免明文长期残留），过期或解析失败视为废弃。
    3. 读完立即 `localStorage.removeItem`。
    4. 关闭「记住密码」时显式将 `historyEntry.password` 写成 `''`，清掉历史残留。
    5. 回填链：`info.value.password || pendingPassword || existingEntry?.password || ''`。
  - 验证：待 ui-tester 复测——勾选「记住密码」登录 → 退出登录 → 登录界面密码栏应自动回填。
  - Owner：frontend-dev

- [x] **ISS-005** `P2` `Room.last_msg_id` 在 `skipPush=true`（stream_end）路径下不更新 — 2026-05-12
  - 根因：stream_end 写库走 `sendMsg` 但 `skipPush=true`,不发 `MessageSendEvent` → `MsgSendConsumer` 不触发 → `RoomDao.refreshActiveTime` 不调用
  - 影响：最近会话列表显示 owner 最后一条而不是 AI 回复
  - 修复(PR #4 commit `104cbbbe` → squash-merge `5f32194b`,方案 A1):4 文件 +37/-6
    - `RoomMapper.java` / `.xml`:新增 `refreshActiveTime` 接口 + IF 单调 SQL(`UPDATE im_room SET last_msg_id = IF(#{msgId} > last_msg_id OR last_msg_id IS NULL, #{msgId}, last_msg_id), active_time = IF(#{msgTime} > active_time OR active_time IS NULL, #{msgTime}, active_time) WHERE id = #{roomId}`),与 ISS-004 ContactMapper 同构
    - `RoomDao.java`:`lambdaUpdate` → `baseMapper.refreshActiveTime`,签名/调用点(`MsgSendConsumer`)零变化,顺带补齐历史缺保护
    - `ChatServiceImpl.java`:注入 `RoomDao`,`sendMsg` 在 `syncContactLastMsgId` 后、`skipPush` gate **前**增补一次推进,删 `TODO(ISS-00X)`
  - 一并修复:RoomDao 历史 `lambdaUpdate` 缺单调保护(sync 与 MsgSendConsumer 异步竞态可旧覆新),IF SQL 现在双路径都保护
  - 评审：reviewer 复核通过(同步调用 vs ISS-006 `@Async` 的非对称性已注明,Room 单行同步开销可忽略,但若 Room 表成为热点表可重新评估)
  - 验证：backend-tester E2E-B(AI stream_end → `im_room.last_msg_id` 推进到 AI 消息 id)与 ISS-004 回归同步进行
- [x] **ISS-006** `P2` 发送者 `im_contact.active_time` 永不在 send 路径推进 — 2026-05-12
  - 根因：`MsgSendConsumer` 不调 `contactDao.refreshOrCreateActiveTime`;唯一调用者 `AckConsumer` 用 `Arrays.asList(dto.getUid())` 只推进 ACK 发起方;group 房 sender 因 `getMemberExceptUidList` 被排除推送 → 永不 ACK 自己消息 → sender contact 永不推进
  - 影响：sender 发完消息后,最近会话列表不冒泡(列表按 `Contact::getActiveTime` 排序,见 `ContactDao.getContactPage`)
  - 修复(PR #3 commit `20790e6c`,方案 A):`ChatServiceImpl.sendMsg` 在 `syncContactLastMsgId` 内同事务调 `contactDao.refreshOrCreateActiveTime(roomId, memberUidList, msgId, LocalDateTime.now())`,全量成员 + ISS-004 IF 单调保护与 AckConsumer 异步路径竞态时不回退
  - 评审：reviewer 复核通过(`syncContactLastMsgId` 方法名在此 PR 后语义扩展,改名留作后续)
  - 副发现:`RetryPushConsumer.java:52` retry 路径 `memberUidList` 取 sender uid 与语义不符 → 抽出 ISS-007 单独跟进
- [x] **ISS-004** `P2` `refreshOrCreateActiveTime` SQL 缺少单调保护 — 2026-05-12
  - 修复（PR #2, commit `ff0906c6` → squash-merge `b9c2be1d`）：`ContactMapper.xml` 中 `last_msg_id` / `active_time` 改为 `IF(#{x} > col OR col IS NULL, #{x}, col)`
  - 评审：reviewer 复核通过，1 文件 12 行纯 SQL 改动
- [x] **ISS-003** `P1` `im_contact.last_msg_id` 未在写入路径自动更新 — 2026-05-12
  - 根因：写入路径（`POST /chat/msg`）只插入 `im_message`，从不更新 `im_contact.last_msg_id`；getMsgList 拉取路径的异步 bump 纯依赖客户端轮询
  - 修复（commit `3663bc6c`）：`ContactMapper.xml` 新增 `INSERT ... ON DUPLICATE KEY UPDATE` + `IF(msgId > last_msg_id OR last_msg_id IS NULL)` 只前进；`ChatServiceImpl.sendMsg` 同事务调用 `syncContactLastMsgId`；撤回天然豁免；stream_end skipPush=true 自动覆盖
  - 评审：manager + reviewer 双重审批（M1/M2/E2 must-fix，S1/S3/I1-I3 should-fix）
  - 验证：backend-tester + server-dev E2E-1（owner sendMsg → 双方 last_msg_id=160471311837696）+ E2E-3（AI stream_end → 双方推进至 160471492192768），零 ERROR/WARN/回滚
  - 遗留：ISS-004（refreshOrCreateActiveTime 无序覆盖）、ISS-005（Room.last_msg_id skipPush）、test infra follow-up（task #13）
- [x] **ISS-001** `P0` 无 AI 回复 — aichat-node 桥接进程未部署 — 2026-05-12
  - 根因:`@aichat/node`(REQ-002 §2.2 的 WS 桥接进程)从未在 `aichat-plugins-dev` 容器中拉起,owner 消息只入 DB,没有 client 接收并触发 openclaw agent 回复(原始 `aichat-claw` 不能直连 HuLa-Server,符合双层架构)。
  - 处置(2026-05-12):
    1. plugin-dev 起草部署改造方案 `shared/环境与部署/配置/aichat-node部署改造方案.md`(方案 A,贴 REQ-002);
    2. server-dev 通过 `POST /api/im/aiclaw/{uid}/refresh-activation` 刷新「安洁」(`uid=140789091499520`) activation token,明文经点对点通道发给 backend-tester(不入 repo);
    3. backend-tester 改 `infra/compose/docker-compose.dev.yml`:supervisor 守护 `node dist/cli.js start` + 新增 `aichat-home` 命名卷(`/root/.aichat`);
    4. backend-tester 执行 §4.1 写 `~/.aichat/config.jsonc` + §4.3 跑 `aichat activate`,supervisor 自动拉起 node;workaround:`claws.openclaw.token` 必须显式填(`detectClawConfig` if 短路 bug,见 §4.1/§九)。
  - 验证:server-dev E2E 发消息 → 「安洁」回「联调测试收到 🐱...流式链路全通路正常」,WS-server 侧 `STREAM_START → DELTA → END`,DB 入库 `id=160454341682176`。
  - 遗留(已建任务,plugin-dev 跟进,不阻塞):①修 `packages/node/src/config.ts:detectClawConfig` token auto-detect 短路;②补 `handler/message.ts:sendToAI` 的 stream 转发日志;③补 `packages/claw/openclaw.plugin.json` 源码侧 `activation.onStartup`;④修 `config.ts:DEFAULT_SERVER_URL` 缺 `/api` 前缀。
- [x] 三仓 push 权限校验通过（2026-03-09）
- [x] **ISS-002** `P1` `/chat/msg/page` 缓存与 DB 不一致（漏返 2 条新消息）— 2026-05-12
  - 根因：`im_contact.last_msg_id` 未随新消息更新，导致游标分页 COUNT 被旧值限制（非 Redis 缓存问题）
  - 处置：backend-tester 清理 72 条 Redis message 缓存 + 更新 last_msg_id=160442597630976
  - 验证：page 接口 total=84 与 DB 一致
