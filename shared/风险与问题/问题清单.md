# 问题清单（Issue Log）

> 规则：问题必须可复现、可归属、可关闭。

## Open
- [ ] **ISS-001** `P0` 无 AI 回复 — aichat-claw 插件代码为骨架实现，缺少消息收发闭环
  - 发现：server-dev 冒烟测试 2026-05-12，消息入库但无 AI 回复
  - 环境就绪：backend-tester 已拉起 `aichat-plugins-dev` 容器 + openclaw gateway，安洁 token 已配置，manifest 缺失 `activation.onStartup` 已修复
  - 代码缺口（需 plugin-dev）：① WebSocket 连接到 HuLa WS 接收消息 ② inbound channel adapter ③ 收到消息后触发 openclaw agent 回复的闭环逻辑
  - 报告：`teamdocs/shared/风险与问题/2026-05-12-runtime冒烟测试-server-dev.md`
  - Owner：plugin-dev → server-dev 复测

## Closed
- [x] 三仓 push 权限校验通过（2026-03-09）
- [x] **ISS-002** `P1` `/chat/msg/page` 缓存与 DB 不一致（漏返 2 条新消息）— 2026-05-12
  - 根因：`im_contact.last_msg_id` 未随新消息更新，导致游标分页 COUNT 被旧值限制（非 Redis 缓存问题）
  - 处置：backend-tester 清理 72 条 Redis message 缓存 + 更新 last_msg_id=160442597630976
  - 验证：page 接口 total=84 与 DB 一致
