# 问题清单（Issue Log）

> 规则：问题必须可复现、可归属、可关闭。

## Open
- [ ] **ISS-001** `P0` 无 AI 回复 — runtime 环境缺少 openclaw 桥接容器（plugins-runtime）
  - 发现：server-dev 冒烟测试 2026-05-12，`POST /chat/msg` 入库正常但无 aiclaw push/stream
  - 根因：`docker ps` 仅有 `hula-server-runtime`，无 `aichat-plugins-runtime`；MsgSendConsumer 检测 aiclaw 不在线则不推送
  - 处置：backend-tester 拉起 `aichat-plugins-runtime` 容器，用安洁 token 注册到 hula-server-runtime:18080
  - 报告：`teamdocs/shared/风险与问题/2026-05-12-runtime冒烟测试-server-dev.md`
  - Owner：backend-tester → server-dev 复测

- [ ] **ISS-002** `P1` `/chat/msg/page` 缓存与 DB 不一致（漏返 2 条新消息）
  - 发现：同一测试中 DB count=83，API total=81，缺失刚发的 2 条
  - 根因：怀疑 Redis 房间消息缓存未失效，缓存最末游标停在 2026-05-11
  - 处置：backend-tester 清理 room=140789095693824 的 Redis 消息缓存键后复测
  - Owner：backend-tester

## Closed
- [x] 三仓 push 权限校验通过（2026-03-09）
